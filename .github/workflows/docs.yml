name: Build documentation

on:
  push:
    branches:
      - documentation
  pull_request:
    branches:
      - documentation
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  docs:
    name: Build docs
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen graphviz

      - name: Install Python dependencies
        run: pip install -r requirements-docs.txt

      - name: Build documentation
        run: bash scripts/build-docs.sh

      # Pages deployment is only needed for direct pushes to the documentation
      # branch. Pull requests still validate the complete Sphinx/Doxygen build
      # without requiring Pages deployment permissions or configuration.
      - name: Configure Pages
        if: github.event_name == 'push' && github.ref == 'refs/heads/documentation'
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/documentation'
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/_build/html

  deploy:
    name: Deploy docs
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: docs
    if: github.event_name == 'push' && github.ref == 'refs/heads/documentation'
    permissions:
      pages: write
      id-token: write
      contents: read
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
